# Python deps for the toolkit's plugins, tests, and bin/ scripts.
# Installed into .venv/ by `just setup` (uv-managed).

# myst-mermaid plugin runtime
pyyaml
jsonschema

# bin/update-dates.py (uses only stdlib + git; no extra deps)
# (intentionally empty — listed here for discoverability)

# Plugin tests
pytest
# Parallel test execution (4 mermaid-cli integration tests each spin up
# their own Chrome; -n auto cuts wall-clock ~3-4× on the CI runner).
pytest-xdist
